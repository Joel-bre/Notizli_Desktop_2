{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window: app commands and events only.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
