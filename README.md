# Notizli Desktop 2

The Notizli desktop recorder, rebuilt: a small Tauri app with a Rust recording
engine for Windows and macOS. It records a meeting on the user's own computer
(room microphone + the call's sound, as two channels), saves it to disk first,
and uploads it to https://notizli.ch for transcription.

Work in progress.
